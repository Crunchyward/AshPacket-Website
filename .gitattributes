# Normalize line endings across operating systems
* text=auto eol=lf

# Windows scripts may use CRLF
*.{cmd,bat} text eol=crlf

# Binary assets
*.{png,jpg,jpeg,gif,ico,webp,svg,pdf,woff,woff2} binary
